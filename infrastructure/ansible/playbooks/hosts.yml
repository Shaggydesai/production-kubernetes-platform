---
- name: Node metrics prerequisites
  hosts: all
  become: true
  roles:
    - node_metrics

- name: etcd backups on the control plane
  hosts: control_plane
  become: true
  roles:
    - etcd_backup
