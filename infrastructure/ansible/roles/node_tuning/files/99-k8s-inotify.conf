# Kubernetes nodes run many file watchers (kubelet, containerd, Longhorn,
# Prometheus, Argo CD). Ubuntu's defaults are too low and produce
# "Too many open files" / "Failed to allocate directory watch".
fs.inotify.max_user_instances = 1024
fs.inotify.max_user_watches = 524288
