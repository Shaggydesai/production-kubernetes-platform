---
- name: Create node-exporter textfile collector directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - /var/lib/node_exporter
    - /var/lib/node_exporter/textfile_collector
