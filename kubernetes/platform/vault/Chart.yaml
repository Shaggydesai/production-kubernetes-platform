apiVersion: v2
name: vault
description: Platform wrapper chart for HashiCorp Vault (production mode, manual unseal)
type: application
version: 0.1.0
appVersion: "1.18.1"
dependencies:
  - name: vault
    version: "0.30.0"
    repository: "https://helm.releases.hashicorp.com"
