apiVersion: v1
kind: Service
metadata:
  name: hermes-demo
  namespace: hermes-lab
spec:
  type: ClusterIP
  selector:
    app: hermes-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
