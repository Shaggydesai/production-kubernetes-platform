apiVersion: v2
name: envoy-gateway
description: Envoy Gateway configuration for the production Kubernetes platform
type: application
version: 0.1.0
appVersion: "v1.8.3"

dependencies:
  - name: gateway-helm
    version: v1.8.3
    repository: "oci://docker.io/envoyproxy"
